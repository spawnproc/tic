[ {n2o, [{protocols,[protocol]},
         {query,protocol},
         {session,protocol},
         {route,protocol}]},
  {kvs, [{dba,kvs_mnesia},
         {dba_st,kvs_st},
         {schema, [ kvs, kvs_stream, book ]} ]} ].
