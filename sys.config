[ {n2o, [{protocols,[protocol]},
         {query,protocol},
         {session,protocol},
         {route,protocol}]},
  {kvs, [{dba,store_mnesia},
         {log_modules,app},
         {log_backend,n2o_log},
         {schema, [ kvs_user, kvs_feed, kvs_subscription, book ]} ]} ].
